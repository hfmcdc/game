services:
  - type: web
    name: nexus-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server/server.js
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
      - key: CLIENT_ORIGIN
        value: "*"
